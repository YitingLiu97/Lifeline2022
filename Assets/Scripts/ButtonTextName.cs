using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;
public class ButtonTextName : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      //  GetComponentInParent<Button>().name = GetComponent<TextMeshProUGUI>().text; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
